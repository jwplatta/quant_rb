require "active_record"
require "rake"
require_relative "config/environment"  # Load your database connection

# Load all Rake tasks from the lib/tasks directory
Dir.glob('lib/tasks/**/*.rake').each { |r| import r }