default: &default
  adapter: postgresql
  encoding: unicode
  pool: 15
  timeout: 5000
  prepared_statements: false
  host: <%= ENV['DATABASE_HOST'] || 'localhost' %>
  port: <%= ENV['DATABASE_PORT'] || 5432 %>
  username: <%= ENV['DATABASE_USER'] || 'postgres' %>
  password: <%= ENV['DATABASE_PASSWORD'] %>

production:
  <<: *default
  database: <%= ENV['DATABASE_NAME'] || 'postgres' %>

development:
  <<: *default
  database: <%= ENV['DATABASE_NAME_DEV'] || 'options_trader_db' %>
  username: <%= ENV['DATABASE_USER_DEV'] || 'postgres' %>
  password: <%= ENV['DATABASE_PASSWORD_DEV'] %>
  host: <%= ENV['DATABASE_HOST_DEV'] || 'localhost' %>
  port: <%= ENV['DATABASE_PORT_DEV'] || 5432 %>

test:
  adapter: postgresql
  encoding: unicode
  pool: 5
  timeout: 5000
  prepared_statements: false
  database: options_trader_db_test
  username: options_trader_test
  password:
  host: localhost
  port: 5432